<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
 PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
<mapper namespace="User">
	<select id="checkUsername" parameterType="string" resultType="_int">
		SELECT COUNT(USER_NAME) FROM TABLE_USER WHERE USER_NAME = #{user_name}
	</select>

	<insert id="join" parameterType="userVO">
		INSERT INTO TABLE_USER
		(USER_NO, USER_NAME, USER_EMAIL, USER_PW, BIRTH_YEAR, USER_STATUS)
		VALUES (SEQ_USER.NEXTVAL, #{user_name}, #{user_email}, #{user_pw}, #{birth_year}, 0)
	</insert>
	
	<select id="login" parameterType="map" resultType="_int">
    	SELECT COUNT(*) FROM TABLE_USER
		WHERE USER_NAME = #{user_name} AND USER_PW = #{user_pw}
   	</select>
   	
   	
</mapper>